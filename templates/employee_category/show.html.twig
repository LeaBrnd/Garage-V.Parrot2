{% extends 'base.html.twig' %}

{% block title %}EmployeeCategory{% endblock %}

{% block body %}
    <h1>EmployeeCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ employee_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ employee_category.Name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_employee_category_index') }}">back to list</a>

    <a href="{{ path('app_employee_category_edit', {'id': employee_category.id}) }}">edit</a>

    {{ include('employee_category/_delete_form.html.twig') }}
{% endblock %}
