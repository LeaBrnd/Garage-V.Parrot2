{% extends 'base.html.twig' %}

{% block title %}EmployeeComment{% endblock %}

{% block body %}
    <h1>EmployeeComment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ employee_comment.id }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ employee_comment.Content }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ employee_comment.CreatedAt ? employee_comment.CreatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_employee_comment_index') }}">back to list</a>

    <a href="{{ path('app_employee_comment_edit', {'id': employee_comment.id}) }}">edit</a>

    {{ include('employee_comment/_delete_form.html.twig') }}
{% endblock %}
